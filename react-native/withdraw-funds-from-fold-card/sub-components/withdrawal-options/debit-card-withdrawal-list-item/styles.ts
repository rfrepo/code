import tw, { style } from 'lib/tailwind'

export const styles = {
  $container: style('bg-cream'),

  $text: style('font-circular-std', 'text-black', 'text-sm'),

  $loadingIndicatorColor: tw.color('black')
}
