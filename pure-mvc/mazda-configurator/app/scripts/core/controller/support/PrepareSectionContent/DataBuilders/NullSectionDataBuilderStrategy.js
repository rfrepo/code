define([], function () {
    'use strict';
    puremvc.define({
            name: 'bmc.controller.support.PrepareSectionContent.DataBuilders.NullSectionDataBuilderStrategy',
            constructor: function (host) {
                this.host = host;
            }
        },
        {
            generateContent: function () {

            }
        });
});